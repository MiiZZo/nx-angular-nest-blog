import { dataTransferObjects } from './data-transfer-objects';

describe('dataTransferObjects', () => {
  it('should work', () => {
    expect(dataTransferObjects()).toEqual('data-transfer-objects');
  });
});
